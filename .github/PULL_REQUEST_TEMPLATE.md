## Summary

<!-- What does this PR do? One or two sentences. -->

## Changes

<!-- Bullet list of key changes. -->

## Test plan

- [ ] `pytest tests/ -q` passes locally (CPU)
- [ ] New tests added for new behaviour / bug fixes
- [ ] No new imports in `src/torch_dfo/` outside of `torch` / stdlib
- [ ] Docstrings updated if public API changed
- [ ] `CHANGELOG.md` entry added (for user-visible changes)

## GPU tested?

<!-- If touching a CUDA path, was it tested on a GPU? Mark N/A if CPU-only change. -->
- [ ] Yes — tested on [device]
- [ ] No — CPU-only change
- [ ] N/A
